---
description: Weekly all-in-one cleanup (refresh-md --all + reconcile --all + duplicates).
argument-hint: "[--repo=<key>]"
---

```bash
work-plan hygiene $ARGUMENTS
```

Relay the output verbatim.
