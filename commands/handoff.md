---
description: Wrap up a work block — capture touched + next + blockers; write session log.
argument-hint: "<track> [--auto-next | --set-next 1,2,3]"
---

```bash
work-plan handoff $ARGUMENTS
```

Relay the output verbatim.
