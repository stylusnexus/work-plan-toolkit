---
description: Run any work-plan subcommand (slot, close, reconcile, group, init-repo, refresh-md, duplicates, canonicalize, list, …).
argument-hint: "<subcommand> [args]"
---

```bash
work-plan $ARGUMENTS
```

Relay the output verbatim.
